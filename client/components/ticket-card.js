const TicketCard = ({ title, price }) => {
    return (
        <div className="w-full p-2 flex flex-col text-md font-md shadow-lg bg-gray-150 text-gray-800 gap-5">
            <p className="m-0">{title}</p>
            <p className="m-0">{title}</p>
        </div>
    )
}